# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# Подпись. У каждого своя, в репозитории ей делать нечего.
*.mobileprovision
*.p12
*.p8

# Выгрузка из Figma: raw.json огромный, рендеры бинарные.
# Разобранный дизайн (screens.json/tokens.json) держим в репозитории —
# по нему видно, из чего собран интерфейс.
design/raw.json
design/png/
# Растровые заливки из макета: то, что нужно приложению, уже лежит
# в ios-native/Sprout/Assets.xcassets.
design/img/

# Секреты. Токен Figma живёт только в окружении.
.env
.env.*
*.local

# Прочее
.DS_Store
.idea/

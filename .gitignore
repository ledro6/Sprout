# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# Подпись. У каждого своя, в репозитории ей делать нечего.
*.mobileprovision
*.p12
*.p8

# Выгрузка из Figma (tool/figma_extract.py) — вся генерируется заново.
# Источник правды — сам макет, а то, что нужно приложению, уже лежит в
# коде и в ios-native/Sprout/Assets.xcassets.
design/

# Секреты. Токен Figma живёт только в окружении.
.env
.env.*
*.local

# Прочее
.DS_Store
.idea/
